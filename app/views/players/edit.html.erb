<h1>Editing Player</h1>

<%= render 'form', player: @player %>
<%= form_for @player do |f| %>
    <%= f.collection_select :team_id, Team.all, :id, :full_name, prompt: true %>
    <%= f.submit %>
<% end %>

<%= link_to 'Show', @player %> |
<%= link_to 'Back', players_path %>
